<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeXX" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>

        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>

        <!--   theme UI controls like checkboxes and text fields e.g. FloatActionButton -->
        <item name="colorAccent">@color/accent</item>

        <!-- drawer menu Title Text Color -->
        <!--<item name="android:textColorPrimary">@color/purple</item>-->

        <!-- color of drawer menu icon and secondary text -->
        <!--<item name="android:textColorSecondary">@color/blue</item>-->

        <!--&lt;!&ndash;window background&ndash;&gt;-->
        <!--<item name="android:windowBackground">@color/window_background</item>-->

    </style>

    <style name="ActionButtonIcon">
        <item name="android:gravity">center</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/ripple</item>

    </style>

    <style name="AppThemeXX.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppThemeXX.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
